帮我用Pyside6写一个工具：

主要是用于Windows桌面的热搜展示，只是**一个**小卡片

核心API功能功能我已写好，你可以参考，相关json文件是调用返回后的内容

卡片定宽定高，鼠标左键按下可拖动，其他按键无反馈

热搜内容放到一个可滚动的子部件中，可以上下滚动，鼠标滚轮

卡片可折叠成一个小按钮在桌面上，点击可展开主界面

背景半透明，所有控件不得加入emoji文本，无边框，不顶置，底置桌面

有相关动画，窗口无边框

显示每个榜的标题（点击榜标题切换排行榜），每条热搜显示标题，用标题+描述作为tooltip，鼠标悬停显示完整内容，点击可打开对应网页

卡片样式简约，具体配置可以用一个json文件配置，方便修改

10分钟刷新一次热搜，可以设置刷新间隔，默认10分钟

添加一个托盘，可自定义图标，也用json配置
托盘可打开工作目录，添加立即刷新的功能，可打开配置文件

日志系统仅记录错误信息，可选择是否开启，默认关闭

给出完整项目代码，以及相关依赖，再生成一个自述文件
